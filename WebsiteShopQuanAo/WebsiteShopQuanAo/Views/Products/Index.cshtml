@model IEnumerable<WebsiteShopQuanAo.Models.ProductItemVM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_MainUserLayout.cshtml";
}

<div class="shop_section shop_reverse">
    <div class="container">
        <div class="row">

            <div class="col-lg-3 col-md-12">

                @Html.Action("Sidebar", "Products")
            </div>

            <div class="col-lg-9 col-md-12">

                <div class="breadcrumb_content">
                    <ul>
                        <li><a href="@Url.Action("Index","Home")">home</a></li>
                        <li>shop</li>
                    </ul>
                </div>

                <div class="shop_banner d-flex align-items-center"
                     data-bgimg="@Url.Content("~/UI_User/assets/img/bg/shop_bg.jpg")">
                    <div class="shop_banner_text">
                        <h2>essential <br /> wears</h2>
                        <p>The collections basic items <br /> essential for all girls</p>
                    </div>
                </div>

                <div class="row shop_wrapper">
                    @if (Model == null || !Model.Any())
                    {
                        <div class="col-12">
                            <div class="section_title text-center mb-130">
                                <h2>No products found</h2>
                            </div>
                        </div>
                    }
                    else
                    {
                        foreach (var sp in Model.SanPham.HINH_ANH_SP)
                        {
                            <div class="col-lg-4 col-md-4 col-sm-6">
                                <div class="single_product">
                                    <div class="product_thumb">
                                        <a href="@Url.Action("Details", "Products", new { id = sp.MaSanPham })">

                                            class="main-img"
                                            src="/assets/img/product/small-product/@sp.TENHINHANH"
                                            alt="">
                                        </a>
                                    </div>

                                    <div class="product_content">
                                        <h3>
                                            <a href="@Url.Action("Details", "Products", new { id = sp.MaSanPham })">
                                                @sp.TenSanPham
                                            </a>
                                        </h3>

                                        <div class="price_box">
                                            <span class="current_price">@String.Format("{0:N0} ₫", sp.GiaBan)</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    }
                </div>

            </div>
        </div>
    </div>
</div>

@section scripts{
    <script>
    $(function () {
        if (!$.ui || !$.ui.slider) return;

        // DEFAULT RANGE (bạn đổi theo DB thực tế)
        var minDefault = 0;
        var maxDefault = 2000000;

        // Lấy min/max hiện tại trên URL để giữ trạng thái
        var url = new URL(window.location.href);
        var minQ = parseInt(url.searchParams.get("min")) || minDefault;
        var maxQ = parseInt(url.searchParams.get("max")) || maxDefault;

        $("#slider-range").slider({
            range: true,
            min: minDefault,
            max: maxDefault,
            step: 50000,
            values: [minQ, maxQ],
            slide: function (event, ui) {
                $("#amount").val(ui.values[0].toLocaleString("vi-VN") + " ₫ - " + ui.values[1].toLocaleString("vi-VN") + " ₫");
                $("#minPrice").val(ui.values[0]);
                $("#maxPrice").val(ui.values[1]);
            }
        });

        // set giá trị lúc load trang
        var initValues = $("#slider-range").slider("values");
        $("#amount").val(initValues[0].toLocaleString("vi-VN") + " ₫ - " + initValues[1].toLocaleString("vi-VN") + " ₫");
        $("#minPrice").val(initValues[0]);
        $("#maxPrice").val(initValues[1]);
    });
    </script>
}

@*<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenSanPham)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MoTa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HinhAnhChinh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NgayTao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChatLieu.TenChatLieu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DanhMuc.TenDanhMuc)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenSanPham)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MoTa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HinhAnhChinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NgayTao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChatLieu.TenChatLieu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DanhMuc.TenDanhMuc)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MaSanPham }) |
                @Html.ActionLink("Details", "Details", new { id=item.MaSanPham }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MaSanPham })
            </td>
        </tr>
    }

    </table>*@
