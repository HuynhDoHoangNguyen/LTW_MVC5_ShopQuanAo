<!DOCTYPE html>
@{
    ViewBag.Title = "Đăng nhập";
    <link rel="stylesheet" href="~/UI_Admin/css/styles.css">

    Layout = "~/Views/Shared/_MainUserLayout.cshtml";
}



<div class="customer_login" style="padding-top: 180px; padding-bottom: 100px;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8">
                <div class="account_form">
                    <h2 class="text-center">Đăng nhập</h2>
                    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <p>
                            <label>Tên đăng nhập <span>*</span></label>
                            <input type="text" name="username" required>
                        </p>
                        <p>
                            <label>Mật khẩu <span>*</span></label>
                            <input type="password" name="password" required>
                        </p>

                        <div class="login_submit">
                            <button type="submit">Đăng nhập</button>
                            <a href="#" class="float-right mt-2">Quên mật khẩu?</a>
                        </div>

                        <div class="mt-4 pt-3 border-top text-center">
                            <span class="text-muted">Bạn là khách hàng mới?</span>
                            <br />
                            <a href="@Url.Action("Register", "Account")" class="d-inline-block mt-2" style="color: #ff6a28; text-decoration: underline; font-weight: bold;">
                                Tạo tài khoản mới
                            </a>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>